package zhihong.algorithm

import zhihong.ZhihongAuthor

@ZhihongAuthor("4test01")
class TwoNum {
    fun twoSum(nums: IntArray, target: Int): IntArray {
        // 快速排序
        // 最小的为a，往后找b，如果找到和为目标就返回，如果找到第一个b+a>target后，a往后移动一个
        return IntArray(1)
    }
}